{erl_opts, [debug_info]}.

%% Dependencies needed by mod_register_invite.erl
{deps,
 [
   %% RFC 4648 Base‑32 encoder/decoder
   {base32, "1.0.0", {hex, base32}},

   %% PNG QR‑code generator (pure Erlang)
   {qrcode, "0.1.5", {hex, qrcode}}
 ].
}